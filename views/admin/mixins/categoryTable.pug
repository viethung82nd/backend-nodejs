mixin categoryTable(items, level)
  each item, index in items
    - const prefix = Array(level + 1).join("-- ")
    tr
        td 
            input(type="checkbox" name='id' value=item.id)
        td #{index + 1}
        td
            img(
                src= item.thumbnail
                alt= item.title
                width="100px"
                height="auto"
            )
        td #{prefix}#{item.title}
        
        td 
          input(
            type='number'
            value=item.position
            style='width: 60px'
            min='1'
            name='position'
          )
        td
            if(item.status == "active")
                a(href="javascript:;"
                  class="badge badge-success"
                  button-change-status 
                  data-status= 'active'
                  data-id= item.id
                  ) Hoạt động
            else 
                a(href="javascript:;" 
                class="badge badge-danger"
                button-change-status 
                data-status= 'inactive'
                data-id= item.id
                ) Dừng hoạt động
        td
            a(href=`/admin/products/detail/${item._id}` class="btn btn-info btn-sm mr-1") Chi tiết
            a(href=`/admin/products/edit/${item._id}` class="btn btn-warning btn-sm") Sửa
            button(class="btn btn-danger btn-sm ml-1" button-delete delete-id= item.id) Xóa
            
    if item.children && item.children.length
      +categoryTable(item.children, level + 1)
    
