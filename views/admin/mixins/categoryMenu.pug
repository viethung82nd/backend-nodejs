mixin categoryMenu(items, level)
  each item in items
    - const prefix = Array(level + 1).join("-- ")
    option(value=item._id)= `${prefix}${item.title}`
    if item.children && item.children.length
      +categoryMenu(item.children, level + 1)
